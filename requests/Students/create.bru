meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{host}}/students
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Teste dos Anjos"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
