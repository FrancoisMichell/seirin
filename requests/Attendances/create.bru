meta {
  name: create attendance
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/attendances
  body: json
  auth: bearer

auth:bearer {
  token: {{SeirinAuthToken}}
}
}

body:json {
  {
    "sessionId": "{{sessionId}}",
    "studentId": "{{studentId}}",
    "status": "present",
    "notes": "On time"
  }
}
