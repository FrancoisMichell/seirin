meta {
  name: Class Sessions
  type: folder
}

docs {
  Endpoints for managing class sessions (individual occurrences of classes).
  
  Class sessions represent scheduled instances of classes on specific dates.
}
