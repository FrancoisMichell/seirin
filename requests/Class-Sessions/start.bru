meta {
  name: start
  type: http
  seq: 11
}

patch {
  url: {{host}}/class-sessions/{{sessionId}}/start
  body: none
  auth: bearer
}

auth:bearer {
  token: {{SeirinAuthToken}}
}

script:post-response {
  // Auto-retry with login if unauthorized
  if (res.getStatus() === 401 && !bru.getVar("isRetry")) {
    console.log("🔒 Token expired. Auto-login and retry...");
    
    const loginRes = await bru.sendRequest({
      url: bru.getEnvVar("host") + "/teacher/login",
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        username: bru.getEnvVar("loginUsername"),
        password: bru.getEnvVar("loginPassword")
      })
    });
    
    if (loginRes.status === 200) {
      const token = loginRes.data.token;
      bru.setEnvVar("SeirinAuthToken", token);
      console.log("✓ Re-authenticated successfully");
      
      bru.setVar("isRetry", true);
      await bru.sendRequest();
    }
  }
  
  if (res.getStatus() === 200) {
    const sessionData = res.getBody();
    console.log(`✓ Session started at ${sessionData.actualStartTime}`);
  }
}

tests {
  test("should return 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("should have actualStartTime set", function() {
    const data = res.getBody();
    expect(data).to.have.property("actualStartTime");
    expect(data.actualStartTime).to.not.be.null;
  });
}

docs {
  Start a class session (sets actualStartTime to now).
  
  Path parameters:
  - sessionId: UUID of the session
  
  Expected response:
  - status: 200 OK
  - body: { "id": "uuid", "actualStartTime": "HH:MM:SS", ... }
}
